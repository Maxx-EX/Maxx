# Maxx 可用函数清单

> 本文档列出当前实际可用的函数（不是文档里宣称的）。
> 按模块分类，标注签名和测试通过情况。

## 标准 IO 模块

| 函数 | 签名 | 状态 |
|------|------|------|
| `io.println` | `(str) -> void` | ✅ 可用 |
| `io.print` | `(str) -> void` | ✅ 可用 |
| `io.read_line` | `() -> str` | ✅ 可用 |
| `io.read_file` | `(str) -> str` | ✅ 可用 |
| `io.write_file` | `(str, str) -> void` | ✅ 可用 |

## 数学模块

| 函数 | 签名 | 状态 |
|------|------|------|
| `sqrt` | `(f64) -> f64` | ✅ 可用 |
| `sin` | `(f64) -> f64` | ✅ 可用 |
| `cos` | `(f64) -> f64` | ✅ 可用 |
| `tan` | `(f64) -> f64` | ✅ 可用 |
| `log` | `(f64) -> f64` | ✅ 可用 |
| `exp` | `(f64) -> f64` | ✅ 可用 |
| `pow` | `(f64, f64) -> f64` | ✅ 可用 |
| `floor` | `(f64) -> f64` | ✅ 可用 |
| `ceil` | `(f64) -> f64` | ✅ 可用 |
| `abs` | `(f64) -> f64` | ✅ 可用 |

## 类型转换

| 函数 | 签名 | 状态 |
|------|------|------|
| `str` | `(T) -> str` | ✅ 可用 |
| `int` | `(T) -> int` | ✅ 可用 |
| `f64` | `(T) -> f64` | ✅ 可用 |

## 时间模块

| 函数 | 签名 | 状态 |
|------|------|------|
| `now` | `() -> f64` | ✅ 可用 |
| `unix_time` | `() -> i64` | ✅ 可用 |
| `sleep` | `(f64) -> void` | ✅ 可用 |

## 字符串模块

| 函数 | 签名 | 状态 |
|------|------|------|
| `str.len` | `(str) -> int` | ✅ 可用 |
| `str.concat` | `(str, str) -> str` | ✅ 可用 |
| `str.sub` | `(str, int, int) -> str` | ✅ 可用 |

## 集合模块

| 函数 | 签名 | 状态 |
|------|------|------|
| `Vec::new` | `(T) -> Vec<T>` | ✅ 可用 |
| `Vec::push` | `(Vec<T>, T) -> void` | ✅ 可用 |
| `Vec::pop` | `(Vec<T>) -> T` | ✅ 可用 |
| `Vec::len` | `(Vec<T>) -> int` | ✅ 可用 |

## Option/Result 类型

| 构造器 | 签名 | 状态 |
|------|------|------|
| `some` | `(T) -> ?T` | ✅ 可用 |
| `none` | `() -> ?T` | ✅ 可用 |
| `ok` | `(T) -> Result<T,E>` | ✅ 可用 |
| `err` | `(E) -> Result<T,E>` | ✅ 可用 |

## 并发模块

| 函数 | 签名 | 状态 |
|------|------|------|
| `spawn` | `(fn) -> task` | ⚠️ 引导阶段 stub |
| `chan::send` | `(chan, T) -> void` | ⚠️ 引导阶段 stub |
| `chan::recv` | `(chan) -> T` | ⚠️ 引导阶段 stub |
